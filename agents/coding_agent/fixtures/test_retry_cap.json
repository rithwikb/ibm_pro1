{
  "_fixture": "test_retry_cap",
  "_scenario": "Retry cap hit. status=needs_retry and retry_count=2. Agent must NOT invoke the LLM. Must escalate to human, preserve existing code_diff, set success: false.",
  "_expected": {
    "status": "awaiting_human_approval",
    "current_agent": "human",
    "code_diff": "--- a/src/auth/login.py\n+++ b/src/auth/login.py\n@@ -42,6 +42,8 @@\n     def login(self, request):\n         username = request.data.get(\"username\")\n         password = request.data.get(\"password\")\n+        if self.rate_limiter.is_exceeded(request.META.get(\"REMOTE_ADDR\", \"\")):\n+            return Response(status=429)\n         user = authenticate(username=username, password=password)\n         if not user:\n             return Response(status=401)\n",
    "retry_count": 2,
    "history_last_entry": {
      "agent": "coding_agent",
      "success": false,
      "output_summary": "retry_count cap reached, escalating to human"
    },
    "llm_must_not_be_invoked": true,
    "immutable_fields_unchanged": ["task_id", "feature_request", "acceptance_criteria", "scoped_files", "plan", "test_results", "review_result", "retry_count"]
  },
  "task_id": "task_001",
  "feature_request": "Add rate limiting to the login endpoint",
  "acceptance_criteria": [
    "Max 5 login attempts per IP per 10 minutes",
    "Returns HTTP 429 when limit exceeded",
    "All existing login tests still pass"
  ],
  "scoped_files": [
    "src/auth/login.py",
    "src/middleware/rate_limit.py"
  ],
  "status": "needs_retry",
  "current_agent": "coding_agent",
  "plan": "Add a token-bucket rate limiter middleware keyed by IP, wrap the login route with it, return 429 on exceed.",
  "history": [
    {
      "agent": "pm_agent",
      "output_summary": "criteria defined",
      "timestamp": "2026-08-10T09:00:00Z",
      "success": true
    },
    {
      "agent": "architect_agent",
      "output_summary": "plan + files scoped",
      "timestamp": "2026-08-10T09:05:00Z",
      "success": true
    },
    {
      "agent": "coding_agent",
      "output_summary": "code_diff generated for 2 file(s)",
      "timestamp": "2026-08-10T09:10:00Z",
      "success": true
    },
    {
      "agent": "review_agent",
      "output_summary": "review failed: 1 finding",
      "timestamp": "2026-08-10T09:15:00Z",
      "success": false
    },
    {
      "agent": "coding_agent",
      "output_summary": "code_diff revised addressing 1 review findings",
      "timestamp": "2026-08-10T09:20:00Z",
      "success": true
    },
    {
      "agent": "review_agent",
      "output_summary": "review failed: 1 finding",
      "timestamp": "2026-08-10T09:25:00Z",
      "success": false
    }
  ],
  "code_diff": "--- a/src/auth/login.py\n+++ b/src/auth/login.py\n@@ -42,6 +42,8 @@\n     def login(self, request):\n         username = request.data.get(\"username\")\n         password = request.data.get(\"password\")\n+        if self.rate_limiter.is_exceeded(request.META.get(\"REMOTE_ADDR\", \"\")):\n+            return Response(status=429)\n         user = authenticate(username=username, password=password)\n         if not user:\n             return Response(status=401)\n",
  "test_results": null,
  "review_result": {
    "passed": false,
    "findings": [
      {
        "checklist_item": "Dependency injection",
        "file": "src/auth/login.py",
        "line": 43,
        "severity": "high",
        "description": "rate_limiter accessed as self.rate_limiter but not injected in __init__ — will raise AttributeError at runtime"
      }
    ]
  },
  "retry_count": 2
}
